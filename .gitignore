# build output
dist/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS
.DS_Store

# jetbrains
.idea/

# typescript build info
tsconfig.tsbuildinfo

# backend (versionado en este monorepo, pero credenciales y deps fuera)
backend/node_modules/
backend/.env

# dumps de DB con datos de producción — nunca a git
backups/

# Sandbox local de modelos para prototipado (ej. .mview de prueba)
# Nunca commitear: los archivos definitivos van al bucket vía /admin upload
public/test-models/

# Storage local DEV (filesystem-based, v3.3.0) — NO se commitea
# En prod el storage es DigitalOcean Spaces, no este directorio.
backend/uploads/

